import { generatedWeatherApiTypes } from './generated-weather-api-types';

describe('generatedWeatherApiTypes', () => {
  it('should work', () => {
    expect(generatedWeatherApiTypes()).toEqual('generated-weather-api-types');
  });
});
